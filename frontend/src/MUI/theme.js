import { createMuiTheme } from "@material-ui/core/styles";

export default createMuiTheme({
  palette: {
    primary: {
      main: "#81d0c8",
    },
    secondary: {
      main: "#000000",
    },
  },
  typography: {},
});
